# Core dependencies (local mode — no Supabase needed)
fastapi>=0.104
uvicorn>=0.24
pydantic>=2.0
requests>=2.31
PyJWT[crypto]>=2.8
python-multipart>=0.0.6

# Cloud mode (add Supabase for auth + hosted DB)
# supabase>=1.2
